# NEXT_SESSION

## Контекст на конец дня
- Прочитаны требования из `AGENTS.md` и `docs/CODEX_SPEC.md`.
- Выбран подход `DB-first`.
- Созданы SQL-скрипты:
    - `database/migrations/001_initial_schema.sql`
    - `database/seeds/001_seed_core.sql`
    - `database/recreate_schema.sql`
- База `automata` создана и заполнена seed-данными.
- Выполнен scaffold EF Core в `Automata.Infrastructure`:
    - `Data/AutomataDbContext.cs`
    - `Data/Entities/Generated/*`
- Предупреждения scaffold про expression indexes (`lower(email)`) — ожидаемы и некритичны при SQL-first подходе.
- Визуальная часть еще не реализована.

## Цель следующей сессии
Начать реализацию **только визуала** (без бизнес-логики и без чтения из БД):
1. Desktop: экран входа/регистрации.
2. Desktop: Главная (5 блоков).
3. Desktop: Администрирование → Торговые автоматы (список + форма) и Компании (список + форма).
4. Web: страница «Монитор ТА».

## Важные ограничения
- Не подключать данные из БД на этом этапе.
- Не реализовывать use cases/auth-логику/валидации на сервере.
- Только статическая верстка и UI-скелет с mock/stub данными.
- UI на русском языке.
- Сохранять текущий стек и структуру проекта.

## Технический подход на визуальный этап
- Desktop:
    - новые `Views` + `ViewModels` с mock-данными;
    - навигация по разделам;
    - статические таблицы/карточки/модальные окна подтверждения;
    - заглушки для фильтров/сортировок/пагинации/CSV-кнопок.
- Web:
    - статическая страница Razor для «Монитор ТА»;
    - фильтры, сводная строка, таблица и сортировки как UI-элементы без реальных API-вызовов.

## Definition of Done (на эту сессию)
- Все основные экраны из спецификации существуют и открываются.
- Видны все требуемые поля/колонки/кнопки (визуально).
- Нет интеграции с БД и внешним API.
- Проект собирается.