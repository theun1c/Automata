Прочитай AGENTS.md, docs/CODEX_SPEC.md и docs/NEXT_SESSION.md.
Продолжи реализацию проекта с этапа визуала (UI-only), без подключения БД и без бизнес-логики.

Сделай:
1) Desktop: вход/регистрация (визуально),
2) Desktop: Главная (5 блоков),
3) Desktop: Администрирование → Торговые автоматы (список+форма) и Компании (список+форма),
4) Web: Монитор ТА (Razor page, UI-only).

Требования:
- UI только на русском.
- Все обязательные поля/колонки/кнопки должны присутствовать по CODEX_SPEC.
- Фильтры/сортировки/пагинация/CSV пока только как элементы интерфейса (stub).
- Не подключай БД, не делай API вызовы, используй mock/stub данные.
- Не меняй стек проекта.
- В конце покажи список измененных файлов и что визуально реализовано по каждому экрану.